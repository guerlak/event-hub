spring:
  application:
    name: event-hub
  datasource:
    url: jdbc:postgresql://localhost:5432/event_db
    username: admin
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true